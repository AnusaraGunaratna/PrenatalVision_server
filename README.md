# PRENATAL_VISION SPRING SERVER 

1. Update Configuration:
   - Go to src/main/resources/application.properties.
   - Enter MongoDB URI, JWT secret key, Python model server URL, and Azure storage details.
2. The server runs at http://localhost:8080 by default.